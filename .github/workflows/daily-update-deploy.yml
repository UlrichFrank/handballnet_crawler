name: Daily Update & Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  schedule:
    # 20:00 UTC (21:00 CET/22:00 CEST) - using cron with daylight saving time offset
    # GitHub Actions interprets cron in UTC, so we use 20:00 UTC as base
    # Note: DST is automatically handled by GitHub Actions
    - cron: '0 20 * * *'
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.10'
  NODE_VERSION: '18'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      # 1. Checkout CODE from main branch
      - name: Checkout code (main branch)
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # 2. Checkout DATA from data branch into subdirectory
      - name: Checkout data (data branch)
        uses: actions/checkout@v4
        with:
          ref: data
          path: _data_branch
          sparse-checkout: frontend/public/data
          fetch-depth: 0

      # 3. Merge data from data branch into working directory
      - name: Merge data from data branch
        shell: bash
        run: |
          echo "📥 Loading previous game data from data branch..."
          if [ -d "_data_branch/frontend/public/data" ]; then
            mkdir -p frontend/public/data
            cp -r _data_branch/frontend/public/data/* frontend/public/data/ 2>/dev/null || true
            echo "✅ Previous game data loaded successfully"
          else
            echo "ℹ️  First run: no previous data to load"
          fi
          
          # Also load error log for retry mechanism
          if [ -f "_data_branch/frontend/public/error_log.json" ]; then
            cp _data_branch/frontend/public/error_log.json frontend/public/error_log.json
            echo "✅ Previous error log loaded for retry mechanism"
          fi
          
          rm -rf _data_branch

      # Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      # Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Setup pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      # Setup pnpm caching
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      # Install Python dependencies
      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # Install Node dependencies
      - name: Install Node dependencies
        run: pnpm install --force

      # Run Scraper
      - name: Run Scraper
        run: |
          echo "📊 Starting scraper..."
          python scraper.py --config config.gh.json
          echo "✅ Scraper completed"

      # Generate Graphics
      - name: Generate Graphics
        run: |
          echo "🎨 Generating graphics..."
          python generate_graphics_from_json.py --config config.gh.json
          echo "✅ Graphics generated"

      # Generate Excel Report
      - name: Generate Excel Report
        run: |
          echo "📋 Generating Excel report..."
          python generate_excel_report.py --config config.gh.json
          echo "✅ Excel report generated"

      # Commit new game data to data branch
      - name: Commit and push game data to data branch
        shell: bash
        run: |
          echo "📊 Comparing data changes..."
          
          # Store paths for comparison
          NEW_DATA="${GITHUB_WORKSPACE}/frontend/public/data"
          OLD_DATA="${GITHUB_WORKSPACE}/_data_branch/frontend/public/data"
          
          # Check for actual differences (data or error log)
          HAS_CHANGES=false
          if [ ! -d "$OLD_DATA" ]; then
            echo "ℹ️  First run: no previous data to compare"
            HAS_CHANGES=true
          else
            NEW_FILES=$(find "$NEW_DATA" -type f 2>/dev/null | wc -l)
            OLD_FILES=$(find "$OLD_DATA" -type f 2>/dev/null | wc -l)
            
            if [ "$NEW_FILES" -ne "$OLD_FILES" ]; then
              echo "✅ File count changed: $OLD_FILES → $NEW_FILES"
              HAS_CHANGES=true
            elif ! diff -r "$OLD_DATA" "$NEW_DATA" > /dev/null 2>&1; then
              echo "✅ Data changes detected"
              HAS_CHANGES=true
            else
              echo "✅ No new game data changes detected"
            fi
          fi
          
          # Also check for error log changes
          if [ -f "${GITHUB_WORKSPACE}/frontend/public/error_log.json" ]; then
            echo "📋 Error log found - will be pushed"
            HAS_CHANGES=true
          fi
          
          # If no changes, skip push and cleanup
          if [ "$HAS_CHANGES" = false ]; then
            echo "✅ No changes to commit"
            rm -rf "${GITHUB_WORKSPACE}/_data_branch"
            exit 0
          fi
          
          # Configure git
          cd "$GITHUB_WORKSPACE"
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          # Push to data branch
          echo "📝 Pushing data to data branch..."
          git fetch origin data
          git checkout data
          
          # Copy game data
          mkdir -p frontend/public/data
          cp -r "$NEW_DATA"/* frontend/public/data/ 2>/dev/null || true
          
          # Copy error log if it exists
          if [ -f "${GITHUB_WORKSPACE}/frontend/public/error_log.json" ]; then
            cp "${GITHUB_WORKSPACE}/frontend/public/error_log.json" frontend/public/error_log.json
            echo "✅ Copied error log"
          fi
          
          # Add changes to git
          git add frontend/public/data/
          if [ -f "frontend/public/error_log.json" ]; then
            git add frontend/public/error_log.json
            echo "📋 Error log added to commit"
          fi
          
          git commit -m "chore: Update game data and error log from scraper [skip ci]"
          git push origin data
          echo "✅ Data and error log pushed to data branch"
          
          # Return to main branch and reload data for build
          git checkout main
          echo "✅ Returned to main branch"
          
          # Reload fresh data from data branch for build step
          echo "📥 Reloading data from data branch for build..."
          mkdir -p /tmp/data_from_branch
          cd /tmp/data_from_branch
          git clone --branch data --depth 1 --single-branch https://github.com/${{ github.repository }}.git . 2>&1 || true
          
          if [ -d "frontend/public/data" ]; then
            mkdir -p "${GITHUB_WORKSPACE}/frontend/public/data"
            cp -r frontend/public/data/* "${GITHUB_WORKSPACE}/frontend/public/data/" 2>/dev/null || true
            echo "✅ Fresh data reloaded from data branch"
          else
            echo "⚠️  No data found in data branch"
          fi
          
          cd "${GITHUB_WORKSPACE}"
          rm -rf /tmp/data_from_branch
          
          # Cleanup
          rm -rf "${GITHUB_WORKSPACE}/_data_branch"

      # Build Frontend
      - name: Build Frontend
        run: |
          echo "🔨 Building frontend..."
          pnpm run build
          echo "✅ Frontend built successfully"
          
          # Copy static assets from public folder (SVGs, icons, etc.)
          echo "📦 Copying static assets..."
          # Copy all SVG files from public to dist
          if [ -d "frontend/public" ]; then
            find frontend/public -maxdepth 1 -name "*.svg" -exec cp {} frontend/dist/ \;
            echo "✅ Copied SVG files"
          fi
          # Also ensure team-logo.svg specifically exists
          if [ -f "frontend/public/team-logo.svg" ]; then
            cp frontend/public/team-logo.svg frontend/dist/team-logo.svg
            echo "✅ Ensured team-logo.svg is in dist"
          fi
          
          # Copy config to dist
          # Use config/config.gh.json for production since public/config.json is git-ignored
          if [ -f "config/config.gh.json" ]; then
            cp config/config.gh.json frontend/dist/config.json
            echo "✅ Copied config/config.gh.json to dist/config.json"
          elif [ -f "frontend/public/config.json" ]; then
            cp frontend/public/config.json frontend/dist/config.json
            echo "✅ Copied frontend/public/config.json to dist/config.json"
          else
            echo "❌ No config file found to copy to dist!"
            # List available config files for debugging
            ls -la config/
            ls -la frontend/public/
          fi
          
          # Copy data directory
          if [ -d "frontend/public/data" ]; then
            cp -r frontend/public/data frontend/dist/data
            echo "✅ Copied data directory to dist/"
          else
            echo "❌ data directory not found in frontend/public!"
          fi
          
          # Copy error log for frontend status page
          if [ -f "frontend/public/error_log.json" ]; then
            cp frontend/public/error_log.json frontend/dist/error_log.json
            echo "✅ Copied error log to dist/"
          fi
          
          # Verify
          echo "📂 Final dist structure:"
          ls -la frontend/dist/

      # Prepare deployment artifacts
      - name: Prepare deployment artifacts
        run: |
          mkdir -p deploy/handballnet_crawler
          
          # Copy everything from frontend/dist
          if [ -d "frontend/dist" ]; then
            cp -r frontend/dist/* deploy/handballnet_crawler/
            echo "✅ Copied frontend dist"
          else
            echo "❌ frontend/dist directory not found!"
          fi
          
          # Copy output (Excel, graphics)
          if [ -d "output" ]; then
            cp -r output deploy/handballnet_crawler/output || echo "❌ Failed to copy output"
            echo "✅ Copied output files"
          fi
          
          echo "📦 Deployment directory ready"
          ls -la deploy/handballnet_crawler/
          echo "---"
          ls -la deploy/handballnet_crawler/data/ 2>/dev/null || echo "No data folder"

      # Deploy to GitHub Pages using GitHub Actions
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './deploy/handballnet_crawler'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # Workflow summary
      - name: Workflow Summary
        if: always()
        run: |
          echo "## ✅ Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Date**: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Deployed to:" >> $GITHUB_STEP_SUMMARY
          echo "- [GitHub Pages](https://ulrichfrank.github.io/handballnet_crawler/)" >> $GITHUB_STEP_SUMMARY
# Trigger rebuild
